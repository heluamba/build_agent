services:
  ai_agent:
    build: .
    container_name: ai_agent
    working_dir: /app
    volumes:
      - .:/app
      - ai_agent_venv:/app/.venv
    env_file:
      - .env
    stdin_open: true
    tty: true

volumes:
  ai_agent_venv: